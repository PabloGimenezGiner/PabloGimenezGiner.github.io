// input/touchHandler.js
// Implementación futura para dispositivos táctiles
export default class TouchHandler {
    constructor(canvas, inputManager) {
        this.canvas = canvas;
        this.inputManager = inputManager;
        console.log("TouchHandler no implementado aún");
    }
}